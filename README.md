## Connecting to the World - React Components

This project demonstrates how to build reusable components using React and TypeScript. I created a reusable Card component that accepts props (title, description, image and link). The component is used multiple times in a parent page to show reusability. The project is built with Vite and styled using Tailwind CSS.
