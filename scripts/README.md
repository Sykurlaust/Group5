# Firestore Listings Import

1. Put your `csvjson.json` file inside the `scripts` folder.
2. Run:

```bash
npm run import:listings
```

3. Open Firebase Console and check Firestore collection `listings`.
